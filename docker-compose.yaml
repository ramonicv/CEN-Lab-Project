services:
  django:
    build: .
    ports:
      - "8000:8000"
    environment:
      DEBUG: 'True'
      SECRET_KEY: 'local-dev-key'
      ALLOWED_HOSTS: 'localhost,127.0.0.1'
    restart: unless-stopped